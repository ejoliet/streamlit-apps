interface:
  display_name: "Streamlit Apps"
  short_description: "Repo-specific patterns and workflows for streamlit-apps"
  default_prompt: "Use the streamlit-apps repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true